<Window x:Class="WordCloud.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WordCloud"
        mc:Ignorable="d"
        FontSize="{StaticResource NormalFontSize}"
        Title="Word Cloud Generator" Height="720" Width="1280" Loaded="Window_Loaded">
    <Grid Margin="4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.382*" MinWidth="256"/>
            <ColumnDefinition Width="0.618*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto" MaxHeight="72"/>
        </Grid.RowDefinitions>
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/WordCloud;component/Resources/checkerboard.png" TileMode="Tile" Opacity="0.20" ViewportUnits="Absolute" Viewport="0,0,32,32"/>
        </Grid.Background>
        <TextBox Grid.Column="0" Grid.Row="0" Margin="4" Name="WordsTextBox" TextWrapping="Wrap" AcceptsReturn="True" 
                 FontFamily="{StaticResource MonoSpaceFamily}" FontSize="{StaticResource MonoSpaceFontSize}"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" 
                 VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <Image Grid.Column="1" Grid.Row="0" Margin="4" Name="WordCloudImage" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" StretchDirection="DownOnly" />
        <WrapPanel Grid.Column="0" Grid.Row="1" Margin="4" VerticalAlignment="Center" HorizontalAlignment="Center" Background="AliceBlue">
            <Button Margin="8" Name="SaveWordsCloud" Content="Save" VerticalAlignment="Center" HorizontalAlignment="Left" Height="32" Width="64" Click="SaveWordsCloud_Click" />
            <Button Margin="8" Name="BuildWordsCloud" Content="Build" VerticalAlignment="Center" HorizontalAlignment="Left" Height="32" Width="64" Click="BuildWordsCloud_Click" />
        </WrapPanel>
        <Grid Grid.Column="1" Grid.Row="1" Margin="4" VerticalAlignment="Center"  HorizontalAlignment="Center" Background="AliceBlue" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" x:Name="CloudWidthLabel" Text="W:" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="4" MinWidth="24" />
            <TextBox Grid.Row="0" Grid.Column="1" x:Name="CloudWidthValue" Text="" VerticalAlignment="Center" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" Margin="4" Width="64" FontFamily="{StaticResource MonoSpaceFamily}" />
            <TextBlock Grid.Row="1" Grid.Column="0" x:Name="CloudHeightLabel" Text="H:" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="4" MinWidth="24" />
            <TextBox Grid.Row="1" Grid.Column="1" x:Name="CloudHeightValue" Text="" VerticalAlignment="Center" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" Margin="4" Width="64" FontFamily="{StaticResource MonoSpaceFamily}" />
            <TextBlock Grid.Row="0" Grid.Column="2" x:Name="CloudFontLabel" Text="Font:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="4" MinWidth="32" />
            <!--<TextBox Grid.Row="0" Grid.Column="3" x:Name="CloudFontValue" Text="" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="4" MinWidth="128" MaxWidth="384"/>-->
            <ComboBox Grid.Row="0" Grid.Column="3" x:Name="CloudFontValue" Text="" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="4" MinWidth="128" MaxWidth="384" IsReadOnly="True"/>
        </Grid>
    </Grid>
</Window>
